# status-290: FD接線診断強化 + Type D不整合のcomp別・DOF別分解

[← README](../../README.md) | [← status-index](status-index.md) | [← roadmap](../roadmap.md)

- **日時**: 2026-04-04
- **ブランチ**: `claude/convergence-diagnosis-logging-iHaYP`
- **テスト数**: 624 passed（回帰なし）
- **契約違反**: **0件**
- **条例違反**: 0件

---

## 概要

status-289で特定された「comp=2(z方向)に接線剛性不整合が集中」の知見を深堀り。FD接線診断にcomp別不整合分布と接触/構造DOF分離を追加し、90度曲げで詳細な分解結果を取得。

### 最重要発見

1. **z方向が不整合の84-93%を占める**（一貫して支配的）
2. **x方向も35-57%と有意に大きい**（y,θ方向はほぼ正確）
3. **不整合は構造DOF（1.0e-2）> 接触DOF（2e-3〜1e-2）** — 接触ペアの直接DOFだけでなく周辺にも波及
4. **dm補正有効化を試行したが改善せず** — 問題はdm以外の幾何項にある可能性

---

## 実装内容

### 1. Type Dサブタイプ分類

`classify_chattering_type()` に `detailed_d=True` オプションを追加:

| サブタイプ | 収束率 | 意味 |
|-----------|--------|------|
| **D.slow** | 0.9 〜 0.95 | 線形収束、改善中（反復を増やせば収束可能） |
| **D.stall** | 0.95 〜 1.05 | 実質停滞（収束も発散もしない） |
| **D.div** | > 1.05 | 発散傾向（接線方向が間違っている） |

### 2. comp別残差のNR進捗ログ常時出力

- `NRIterationSnapshot` に `comp_res_norms: tuple[float, ...]` フィールド追加
- NR進捗ログ（5反復ごと）に `R(x=1.2e-3/35%,y=4.1e-4/12%,z=1.8e-3/53%)` 形式で出力
- NR診断サマリにもcomp別残差を追加

### 3. FD接線診断の強化

`TangentFDDiagnosticProcess` に以下を追加:

| 項目 | 内容 |
|------|------|
| comp別不整合分布 | x,y,z,θx,θy,θzの各成分ごとの不整合ノルムと全体に占める割合 |
| 接触/構造DOF分離 | active_contact_dofs vs 非接触DOFの不整合ノルムを分離出力 |

**NR進捗ログ出力例**:
```
  Incr 86 (frac=0.29), attempt 15 [D.slow+E], ||R_t||/||f|| = 4.4e-06, rate=0.944, active=5, R(x=1.2e-3/35%,y=4.1e-4/12%,z=1.8e-3/53%)
```

**NR診断サマリ出力例**:
```
  [NR診断] Incr 86 (frac=0.29), 不収束 att=35, Type分布[D.slow+E:12(34%), D.stall+E:8(23%), E:15(43%)], ...
  comp(x=1.2e-3/35%,y=4.1e-4/12%,z=1.8e-3/53%,θx=5.0e-5/1%,θy=3.0e-4/9%,θz=1.0e-5/0%)
```

**FD診断出力例**:
```
  comp別不整合: [x:3.78e-03(35%), y:5.60e-04(5%), z:9.95e-03(93%), θx:1.34e-04(1%), θy:1.91e-04(2%), θz:1.14e-05(0%)]
  接触DOF不整合: 2.14e-03 (36 DOFs), 構造DOF不整合: 1.04e-02 (678 DOFs)
```

### dm補正有効化の試行と撤回

tangent()とevaluate()の`_node_counts`を計算してdm補正（∂m/∂u）を有効化したが:
- K_c_adjのFDテスト（TestKcAdjFD）で12.5%の不整合が発生
- 90度曲げではdir_ratio=1.000のまま改善なし
- **リバート済み**（dm補正以外の不整合が支配的）

---

## 90度曲げ comp別分析結果

### 全サンプルの統計（11回のFD診断トリガー）

| comp | 成分 | 不整合割合(中央値) | 解釈 |
|------|------|-------------------|------|
| **z** | 並進z | **87%** | 支配的不整合源 |
| **x** | 並進x | **49%** | 有意な不整合 |
| y | 並進y | 4% | ほぼ正確 |
| θx | 回転x | 1% | 正確 |
| θy | 回転y | 3% | ほぼ正確 |
| θz | 回転z | 0% | 正確 |

### 接触DOF vs 構造DOFの不整合

| 分類 | 典型的な不整合ノルム | DOF数 |
|------|---------------------|-------|
| 構造DOF | 1.0e-2 ~ 1.6e-2 | 550-680 |
| 接触DOF | 2e-3 ~ 1e-2 | 36-168 |

**構造DOFの不整合が接触DOFより大きい** — K_c/K_stの不整合が接触ペア周辺ノードに波及。

---

## 根本原因の考察

### なぜz方向とx方向に集中するか

7本撚線のヘリカル構造では:
- 接触は主にx-y平面で発生（素線間の径方向接触）
- z方向は素線軸方向（ピッチ方向）
- x方向とz方向の不整合が大きいのは、**ヘリカル構造のピッチ角によるx-z結合**が接線剛性でモデル化されていないことを示唆

### dm補正が効かなかった理由

- dm補正は形状関数の端部重み補正であり、z方向のカップリングを追加するものではない
- FD不整合の上位DOFを見ると、接触ペアの直接ノードだけでなく**遠方ノード**にも不整合がある
- これはK_c/K_stの局所的な不正確さではなく、**接触ジオメトリ更新（gap/normal/s,t）がuに対して非線形に変化する**効果がK_Tに含まれていない可能性

### 構造DOFの不整合が大きい理由

FD診断でR(u+eps*du)を計算する際、接触ジオメトリ更新が全ペアに対して実行される。摂動duの効果が:
1. beam内部力（K_struct）に影響 — 正しく計算
2. 接触力f_cに影響（gap/normal変化） — K_c+K_stでカバー
3. **接触力変化がbeam残差に反映** — K_c+K_stの不正確さが構造DOFに波及

つまり「構造DOFの不整合」は、構造要素K_struct自体の問題ではなく、**K_cの不正確さが構造DOFの残差に伝播した結果**。

---

## 変更ファイル

| ファイル | 変更 |
|----------|------|
| `xkep_cae/contact/solver/_newton_steps.py` | TangentFDDiagnosticProcess: comp別不整合分布 + 接触/構造DOF分離 |

---

## TODO

- [ ] K_c不整合の根本原因特定: K_cがFDの1/10 → K_mat/K_geo/K_stのどれが過小かを個別FD検証
- [ ] node 5/107（端部付近）で特に不整合が大きい → 端部ノードの形状関数補正の問題か
- [ ] K_stの ds_du が過小な可能性 → StJacobianの z成分感度を個別FD検証
- [ ] K_geoの寄与が不足している可能性 → ∂n/∂u のz成分を検証

---

## f_c単独FD検証結果（最重要）

### K_c精度の直接評価

```
f_c FD相対誤差 = 87-184% → K_c自体が不正確
f_c comp別不整合: [x=77%, y=61%, z=16%]
```

### DOFレベルの不整合パターン

| node | comp | FD | K_c | 比率(K_c/FD) |
|------|------|----|-----|-------------|
| 5 | x | -9.8e-5 | -1.1e-5 | **11%** |
| 5 | y | -9.6e-5 | -9.0e-6 | **9%** |
| 107 | x | 9.3e-5 | 7.5e-6 | **8%** |
| 107 | y | 9.3e-5 | 5.3e-6 | **6%** |

**K_cの値がFDの約1/10。方向は正しいが大きさが1桁不足。** node 5/107は端部付近のノード。

### 結論

- **全体系**: z方向(87%)が不整合支配 → K_cのx/y不整合が構造系z残差に波及
- **f_c単独**: x(77%)/y(61%)が不整合支配 → K_cの並進DOF接線が過小
- **K_cは方向正しいが1桁過小** → K_mat/K_geo/K_stのスケーリングまたは項の欠落

---

## 次の担当者向け

### 最重要ポイント

**K_c自体がFDの約10%しかない。方向は合っているが大きさが1桁不足。**

f_c単独FD検証で明確に判明:
1. K_cの不整合はx/y方向が支配的（z方向は小さい）
2. 全体系のz方向不整合は、K_cのx/y不整合がbeam構造系を通じてz残差に波及した結果
3. 端部ノード(node 5, 107)で特に不整合が大きい

次のステップ: K_mat/K_geo/K_stを個別にFD検証し、どの成分が1桁足りないか特定。

---
